#pragma once

namespace CXX_ISRS
{
    enum ExceptionType
    {
        INFO,
        WARNING,
        ERROR
    };

    enum UserStatus
    {
        ACTIVE,
        BLOCKED
    };

    enum DialogType
    {
        EXIT,
        LOGOFF
    };
}
